# Architecture Specification

**Project**: {{ project_name }}
**Version**: {{ version }}
**Generated**: {{ date }}

---

## 1. System Overview

{{ overview }}

## 2. Architecture Patterns

- {{ pattern_1 }} (e.g., Single Page Application)
- {{ pattern_2 }} (e.g., REST API via Supabase)
- {{ pattern_3 }} (e.g., Row-Level Security for multi-tenancy)

## 3. Directory Structure

```
{{ project_slug }}/
├── src/
│   ├── components/
│   │   ├── ui/              # Reusable primitives: Button, Input, Modal, Card
│   │   └── features/        # Feature-specific components grouped by domain
│   ├── pages/               # Route-level page components
│   ├── hooks/               # Custom React hooks
│   ├── lib/
│   │   ├── supabase.ts      # Supabase client initialization
│   │   └── api.ts           # Type-safe API wrappers
│   ├── types/
│   │   ├── index.ts         # Application interfaces and types
│   │   └── database.ts      # Supabase generated types
│   └── utils/               # Pure utility functions
├── supabase/
│   ├── functions/           # Edge Functions
│   └── migrations/          # SQL migration files
├── tests/
│   └── e2e/                 # Playwright tests
├── public/
├── vercel.json
├── vite.config.ts
├── tailwind.config.ts
└── playwright.config.ts
```

## 4. Component Architecture

### {{ component_name }}
- **Path**: `src/components/{{ domain }}/{{ component_name }}.tsx`
- **Purpose**: {{ purpose }}
- **Props**: `{{ props_interface }}`
- **Data**: {{ data_fetching_strategy }}
- **Dependencies**: {{ dependencies }}

## 5. Routing Structure

```
/                        → HomePage (public)
/login                   → LoginPage (public)
/signup                  → SignupPage (public)
/dashboard               → DashboardPage (auth required)
/{{ resource }}          → {{ Resource }}ListPage (auth required)
/{{ resource }}/:id      → {{ Resource }}DetailPage (auth required)
/settings                → SettingsPage (auth required)
/404                     → NotFoundPage (public)
```

## 6. API Contracts

### `{{ METHOD }} /api/{{ resource }}`

**Request**:
```json
{{ request_body }}
```

**Response 200**:
```json
{{ response_200 }}
```

**Response 400**:
```json
{ "error": { "code": "VALIDATION_ERROR", "message": "Description of what failed" } }
```

## 7. Database Schema

### `{{ table_name }}`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | UUID | PK, DEFAULT gen_random_uuid() | Primary key |
| user_id | UUID | NOT NULL, FK auth.users | Owner |
| {{ field }} | {{ type }} | {{ constraints }} | {{ description }} |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() | |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() | |

**RLS**: Users can only access their own records (`auth.uid() = user_id`).

## 8. State Management

| State Type | Solution | Example |
|------------|----------|---------|
| Server data | TanStack Query | `useQuery({ queryKey: ['items'], queryFn: fetchItems })` |
| Local UI | useState / useReducer | Modal open/close, form state |
| Auth | Supabase client | `supabase.auth.getUser()` |

## 9. Environment Variables

| Variable | Description | Required |
|----------|-------------|----------|
| `VITE_SUPABASE_URL` | Supabase project URL | Yes |
| `VITE_SUPABASE_ANON_KEY` | Supabase public key | Yes |

## 10. Performance Targets

| Metric | Target |
|--------|--------|
| LCP (Largest Contentful Paint) | < 2.5s |
| FID (First Input Delay) | < 100ms |
| CLS (Cumulative Layout Shift) | < 0.1 |
| Bundle size (gzipped) | < 500KB |

---
*Generated by Autonomous Software Factory — {{ timestamp }}*
